@page
@model VisualizarModel
@{
    ViewData["Title"] = "Home page";
}
@using CRUD.Models

<div class="text-center">
    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Nome</th>
                <th>Telefone</th>
                <th>Data</th>
                <th>Observações</th>
                <th>Botões</th>
            </tr>
        </thead>

        <tbody>

            @foreach(Agendamento agendamento in Model.ListaAgendamentos)
            {
                <tr>
                    <td>
                        @agendamento.Id
                    </td>

                    <td>
                        @agendamento.Nome
                    </td>

                    <td>
                        @agendamento.Telefone
                    </td>

                    <td>
                        @agendamento.DataAgendamento
                    </td>

                    <td>
                        @agendamento.Observacoes
                    </td>

                    <td>
                        <a asp-page="/Editar" asp-route-id="@agendamento.Id">Editar</a>

                        <a asp-page="/Deletar" asp-route-id="@agendamento.Id">Deletar</a>
                    </td>
                </tr>
            }

        </tbody>
    </table>
</div>
